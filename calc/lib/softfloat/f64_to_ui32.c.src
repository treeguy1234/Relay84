	section	.text,"ax",@progbits
	assume	adl = 1
	section	.text,"ax",@progbits
	public	___f64_to_ui32
___f64_to_ui32:
	ld	hl, -12
	call	__frameset
	ld	hl, (ix + 6)
	ld	de, (ix + 9)
	ld	bc, (ix + 12)
	ld	iy, 52
	push	iy
	call	__llshru
	pop	de
	ld.sis	bc, 2047
	call	__sand
	ex	de, hl
	ld	iyl, e
	ld	iyh, d
	ex	de, hl
	ld	hl, 15
	push	hl
	scf
	sbc	hl, hl
	push	hl
	push	hl
	ld	hl, (ix + 6)
	ld	de, (ix + 9)
	ld	bc, (ix + 12)
	call	__lland
	ld	(ix - 3), hl
	pop	hl
	pop	hl
	pop	hl
	ld	(ix - 12), iy
	ex	de, hl
	ld	e, iyl
	ld	d, iyh
	ex	de, hl
	add.sis	hl, bc
	or	a, a
	sbc.sis	hl, bc
	jr	z, BB0_2
	ld	a, 0
	jr	BB0_3
BB0_2:
	ld	a, -1
BB0_3:
	ld	hl, 16
	push	hl
	or	a, a
	sbc	hl, hl
	push	hl
	push	hl
	ld	hl, (ix - 3)
	ld	(ix - 6), de
	ld	(ix - 9), bc
	call	__lladd
	pop	iy
	pop	iy
	pop	iy
	bit	0, a
	jr	nz, BB0_5
	ld	(ix - 3), hl
BB0_5:
	bit	0, a
	jr	nz, BB0_7
	ld	(ix - 6), de
BB0_7:
	bit	0, a
	ld	iy, (ix - 3)
	jr	nz, BB0_9
	ld	l, c
	ld	h, b
	ld	(ix - 9), hl
BB0_9:
	ld.sis	de, 1063
	ld	bc, (ix - 12)
	ld	l, c
	ld	h, b
	or	a, a
	sbc.sis	hl, de
	jp	nc, BB0_11
	ld.sis	hl, 1063
	or	a, a
	sbc.sis	hl, bc
	ld	de, 0
	ld	e, l
	ld	d, h
	or	a, a
	sbc	hl, hl
	push	hl
	push	de
	ld	hl, (ix - 9)
	push	hl
	ld	hl, (ix - 6)
	push	hl
	push	iy
	call	_softfloat_shiftRightJam64
	push	hl
	pop	iy
	ld	(ix - 6), de
	ld	(ix - 9), bc
	pop	hl
	pop	hl
	pop	hl
	pop	hl
	pop	hl
BB0_11:
	ld	a, (ix + 18)
	ld	hl, (ix + 6)
	ld	de, (ix + 9)
	ld	bc, (ix + 12)
	call	__llcmpzero
	jp	m, BB0_13
	ld	l, 0
	jr	BB0_14
BB0_13:
	ld	l, 1
BB0_14:
	ld	bc, (ix - 9)
	ld	e, a
	push	de
	ld	e, (ix + 15)
	push	de
	push	bc
	ld	de, (ix - 6)
	push	de
	push	iy
	rrc	l
	sbc	hl, hl
	push	hl
	call	_softfloat_roundToUI32
	ld	sp, ix
	pop	ix
	ret
	section	.text,"ax",@progbits

	ident	"clang version 15.0.7 (https://github.com/CE-Programming/llvm-project 31b787336940cbc96d054033c3d843f344507042)"
	extern	__Unwind_SjLj_Register
	extern	__Unwind_SjLj_Unregister
	extern	__frameset
	extern	__llshru
	extern	_softfloat_roundToUI32
	extern	__lladd
	extern	__llcmpzero
	extern	__sand
	extern	_softfloat_shiftRightJam64
	extern	__lland
