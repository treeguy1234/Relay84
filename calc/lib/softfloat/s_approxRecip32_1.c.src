	section	.text,"ax",@progbits
	assume	adl = 1
	section	.text,"ax",@progbits
	public	_softfloat_approxRecip32_1
_softfloat_approxRecip32_1:
	ld	hl, -12
	call	__frameset
	ld	bc, (ix + 6)
	ld	de, 15
	ld	iy, _softfloat_approxRecip_1k0s
	ld	l, 27
	ld	a, (ix + 9)
	call	__lshru
	push	bc
	pop	hl
	push	de
	pop	bc
	call	__iand
	push	hl
	pop	de
	ld	l, 11
	ld	bc, (ix + 6)
	ld	a, (ix + 9)
	call	__lshru
	ld	(ix - 6), bc
	ld	c, 1
	ex	de, hl
	call	__ishl
	push	hl
	pop	de
	add	iy, de
	ld	hl, (iy)
	ld	(ix - 3), hl
	ld	hl, _softfloat_approxRecip_1k1s
	add	hl, de
	ld	hl, (hl)
	ld	iy, 0
	ex	de, hl
	ld	iyl, e
	ld	iyh, d
	ex	de, hl
	ld	hl, (ix - 6)
	ld	e, a
	ld	bc, 65535
	xor	a, a
	call	__land
	lea	bc, iy
	call	__lmulu
	push	hl
	pop	bc
	ld	a, e
	ld	l, 20
	call	__lshru
	ld	hl, (ix - 3)
	or	a, a
	sbc.sis	hl, bc
	ld	de, 0
	push	de
	pop	iy
	ex	de, hl
	ld	iyl, e
	ld	iyh, d
	ex	de, hl
	ld	(ix - 12), iy
	ld.sis	bc, 0
	or	a, a
	sbc	hl, hl
	ld	l, (ix + 9)
	push	de
	push	hl
	ld	hl, (ix + 6)
	push	hl
	lea	hl, iy
	call	__llmulu
	pop	iy
	pop	iy
	pop	iy
	ld	iy, 7
	push	iy
	call	__llshru
	pop	bc
	call	__lnot
	ld	(ix - 6), hl
	ld	l, 16
	ld	iy, (ix - 12)
	lea	bc, iy
	xor	a, a
	call	__lshl
	ld	(ix - 3), bc
	ld	bc, 0
	ld	c, e
	ld	(ix - 9), bc
	or	a, a
	sbc	hl, hl
	push	hl
	push	hl
	push	iy
	ld	hl, (ix - 6)
	push	bc
	pop	de
	ld.sis	bc, 0
	call	__llmulu
	pop	iy
	pop	iy
	pop	iy
	ld	iy, 24
	push	iy
	call	__llshru
	push	hl
	pop	bc
	pop	hl
	ld	d, e
	ld	hl, (ix - 3)
	ld	e, a
	ld	a, d
	call	__ladd
	ld	(ix - 3), hl
	ld	a, e
	or	a, a
	sbc	hl, hl
	push	hl
	ld	de, (ix - 9)
	push	de
	ld	hl, (ix - 6)
	push	hl
	ld.sis	bc, 0
	call	__llmulu
	pop	iy
	pop	iy
	pop	iy
	ld	iy, 32
	push	iy
	call	__llshru
	ld	(ix - 6), hl
	pop	iy
	or	a, a
	sbc	hl, hl
	ld	l, a
	ld	iy, 0
	push	iy
	push	hl
	ld	hl, (ix - 3)
	push	hl
	ld	hl, (ix - 6)
	call	__llmulu
	pop	iy
	pop	iy
	pop	iy
	ld	iy, 48
	push	iy
	call	__llshru
	push	hl
	pop	bc
	pop	hl
	ld	d, e
	ld	hl, (ix - 3)
	ld	e, a
	ld	a, d
	call	__ladd
	ld	sp, ix
	pop	ix
	ret
	section	.text,"ax",@progbits

	ident	"clang version 15.0.7 (https://github.com/CE-Programming/llvm-project 31b787336940cbc96d054033c3d843f344507042)"
	extern	__Unwind_SjLj_Register
	extern	__Unwind_SjLj_Unregister
	extern	__lshl
	extern	__frameset
	extern	__llshru
	extern	__iand
	extern	_softfloat_approxRecip_1k1s
	extern	__lmulu
	extern	__lnot
	extern	__land
	extern	__llmulu
	extern	__lshru
	extern	__ishl
	extern	__ladd
	extern	_softfloat_approxRecip_1k0s
