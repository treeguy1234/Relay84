	section	.text,"ax",@progbits
	assume	adl = 1
	section	.text,"ax",@progbits
	public	_softfloat_propagateNaNF32UI
_softfloat_propagateNaNF32UI:
	ld	hl, -4
	call	__frameset
	ld	de, (ix + 6)
	ld	a, (ix + 9)
	ld	(ix - 4), a
	ld	l, a
	push	hl
	ld	(ix - 3), de
	push	de
	call	_softfloat_isSigNaNF32UI
	pop	hl
	pop	hl
	bit	0, a
	jr	z, BB0_2
	ld	hl, ___fe_cur_env
	set	4, (hl)
	jr	BB0_8
BB0_2:
	ld	de, (ix + 12)
	ld	l, (ix + 15)
	push	hl
	push	de
	call	_softfloat_isSigNaNF32UI
	pop	hl
	pop	hl
	bit	0, a
	jr	z, BB0_4
	ld	hl, ___fe_cur_env
	set	4, (hl)
BB0_4:
	ld	l, (ix + 9)
	push	hl
	ld	hl, (ix + 6)
	push	hl
	call	_isNaNF32UI
	pop	hl
	pop	hl
	bit	0, a
	jr	nz, BB0_6
	ld	hl, (ix + 12)
	ld	(ix - 3), hl
BB0_6:
	bit	0, a
	jr	nz, BB0_8
	ld	a, (ix + 15)
	ld	(ix - 4), a
BB0_8:
	ld	hl, (ix - 3)
	ld	e, (ix - 4)
	ld	bc, 4194304
	xor	a, a
	call	__lor
	ld	sp, ix
	pop	ix
	ret
	section	.text,"ax",@progbits

	ident	"clang version 15.0.7 (https://github.com/CE-Programming/llvm-project 31b787336940cbc96d054033c3d843f344507042)"
	extern	__Unwind_SjLj_Register
	extern	__lor
	extern	__Unwind_SjLj_Unregister
	extern	__frameset
	extern	_softfloat_isSigNaNF32UI
	extern	_isNaNF32UI
	extern	___fe_cur_env
