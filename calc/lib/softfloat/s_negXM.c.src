	section	.text,"ax",@progbits
	assume	adl = 1
	section	.text,"ax",@progbits
	public	_softfloat_negXM
_softfloat_negXM:
	ld	hl, -6
	call	__frameset
	ld	iy, (ix + 9)
	ld	a, 1
	ld	bc, 0
	push	bc
	pop	hl
	ld	l, (ix + 6)
	dec	hl
	ld	(ix - 3), hl
BB0_1:
	ld	(ix - 6), iy
	ld	hl, (iy)
	lea	iy, iy + 3
	ld	e, (iy)
	call	__lnot
	ld	d, a
	ld	c, a
	xor	a, a
	call	__ladd
	push	hl
	pop	bc
	ld	iy, (ix - 6)
	ld	(iy), bc
	ld	(iy + 3), e
	ld	hl, (ix - 3)
	add	hl, bc
	or	a, a
	sbc	hl, bc
	jr	z, BB0_5
	push	bc
	pop	hl
	call	__lcmpzero
	ld	a, d
	jr	z, BB0_4
	ld	d, 0
	ld	a, d
BB0_4:
	ld	hl, (ix - 3)
	dec	hl
	ld	(ix - 3), hl
	ld	iy, (ix - 6)
	lea	iy, iy + 4
	ld	bc, 0
	jr	BB0_1
BB0_5:
	ld	sp, ix
	pop	ix
	ret
	section	.text,"ax",@progbits

	ident	"clang version 15.0.7 (https://github.com/CE-Programming/llvm-project 31b787336940cbc96d054033c3d843f344507042)"
	extern	__Unwind_SjLj_Register
	extern	__Unwind_SjLj_Unregister
	extern	__frameset
	extern	__lcmpzero
	extern	__lnot
	extern	__ladd
