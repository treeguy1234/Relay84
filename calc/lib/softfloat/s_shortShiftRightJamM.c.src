	section	.text,"ax",@progbits
	assume	adl = 1
	section	.text,"ax",@progbits
	public	_softfloat_shortShiftRightJamM
_softfloat_shortShiftRightJamM:
	ld	hl, -20
	call	__frameset
	ld	bc, (ix + 9)
	ld	l, (ix + 12)
	ld	a, l
	neg
	ld	(ix - 6), a
	ld	de, 0
	ld	e, (ix + 6)
	ld	(ix - 3), de
	push	bc
	pop	iy
	ld	de, (iy)
	ld	a, (iy + 3)
	push	de
	pop	iy
	lea	bc, iy
	ld	d, a
	call	__lshru
	ld	(ix - 9), bc
	ld	(ix - 10), a
	call	__lshl
	push	bc
	pop	hl
	ld	e, a
	lea	bc, iy
	ld	a, d
	call	__lcmpu
	jr	nz, BB0_2
	ld	bc, 0
	jr	BB0_3
BB0_2:
	ld	bc, 1
BB0_3:
	ld	hl, (ix - 3)
	dec	hl
	ld	(ix - 3), hl
	ld	hl, (ix - 9)
	ld	e, (ix - 10)
	xor	a, a
	call	__lor
	ld	(ix - 9), hl
	ld	(ix - 10), e
	ld	a, (ix - 6)
	and	a, 31
	ld	(ix - 14), a
	ld	iy, (ix + 9)
	lea	de, iy + 4
	ld	hl, (ix - 3)
	ld	bc, (ix + 15)
	ld	(ix - 6), bc
BB0_4:
	add	hl, bc
	or	a, a
	sbc	hl, bc
	jr	z, BB0_6
	push	de
	pop	iy
	ld	(ix - 20), iy
	ld	bc, (iy)
	ld	(ix - 13), bc
	ld	d, (iy + 3)
	ld	(ix - 17), hl
	ld	bc, (ix - 13)
	ld	a, d
	ld	l, (ix - 14)
	call	__lshl
	push	bc
	pop	hl
	ld	e, a
	ld	bc, (ix - 9)
	ld	a, (ix - 10)
	call	__lor
	ld	iy, (ix - 6)
	ld	(iy), hl
	ld	(iy + 3), e
	ld	bc, (ix - 13)
	ld	a, d
	ld	l, (ix + 12)
	call	__lshru
	ld	(ix - 9), bc
	ld	hl, (ix - 17)
	ld	(ix - 10), a
	ld	iy, (ix - 20)
	lea	iy, iy + 4
	lea	de, iy
	ld	iy, (ix - 6)
	lea	iy, iy + 4
	ld	(ix - 6), iy
	dec	hl
	jr	BB0_4
BB0_6:
	ld	c, 2
	ld	hl, (ix - 3)
	call	__ishl
	push	hl
	pop	bc
	ld	hl, (ix + 15)
	push	hl
	pop	iy
	add	iy, bc
	ld	hl, (ix - 9)
	ld	(iy), hl
	ld	a, (ix - 10)
	ld	(iy + 3), a
	ld	sp, ix
	pop	ix
	ret
	section	.text,"ax",@progbits

	ident	"clang version 15.0.7 (https://github.com/CE-Programming/llvm-project 31b787336940cbc96d054033c3d843f344507042)"
	extern	__Unwind_SjLj_Register
	extern	__lor
	extern	__Unwind_SjLj_Unregister
	extern	__lshl
	extern	__frameset
	extern	__lcmpu
	extern	__lshru
	extern	__ishl
