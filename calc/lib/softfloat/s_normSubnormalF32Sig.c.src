	section	.text,"ax",@progbits
	assume	adl = 1
	section	.text,"ax",@progbits
	public	_softfloat_normSubnormalF32Sig
_softfloat_normSubnormalF32Sig:
	call	__frameset0
	ld	iy, (ix + 6)
	ld	hl, (ix + 9)
	ld	e, (ix + 12)
	call	__lctlz
	add	a, -8
	ld	c, a
	rlc	a
	sbc	a, a
	ld	b, a
	ld.sis	hl, 1
	or	a, a
	sbc.sis	hl, bc
	ld	(iy), l
	ld	(iy + 1), h
	ld	a, c
	rlc	a
	sbc	hl, hl
	push	hl
	pop	de
	ld	e, c
	push	de
	pop	hl
	push	de
	pop	iy
	add	iy, iy
	sbc	hl, hl
	push	hl
	pop	bc
	ex	de, hl
	ld	e, c
	ld	bc, -1
	xor	a, a
	call	__land
	ld	bc, (ix + 9)
	ld	a, (ix + 12)
	call	__lshl
	ld	iy, (ix + 6)
	ld	(iy + 2), bc
	ld	(iy + 5), a
	lea	hl, iy
	pop	ix
	ret
	section	.text,"ax",@progbits

	ident	"clang version 15.0.7 (https://github.com/CE-Programming/llvm-project 31b787336940cbc96d054033c3d843f344507042)"
	extern	__Unwind_SjLj_Register
	extern	__Unwind_SjLj_Unregister
	extern	__lshl
	extern	__lctlz
	extern	__land
	extern	__frameset0
