	section	.text,"ax",@progbits
	assume	adl = 1
	section	.text,"ax",@progbits
	public	_softfloat_shiftRightJamM
_softfloat_shiftRightJamM:
	ld	hl, -13
	call	__frameset
	ld	d, (ix + 6)
	ld	hl, (ix + 12)
	ld	e, (ix + 15)
	ld	bc, 32
	xor	a, a
	ld	iy, 0
	call	__lcmpu
	jr	nc, BB0_2
	or	a, a
	sbc	hl, hl
	ld	l, d
	ld	(ix - 10), a
	ld	(ix - 6), iy
	lea	de, iy
	jr	BB0_8
BB0_2:
	ld	iyl, 5
	push	hl
	pop	bc
	ld	a, e
	ex	de, hl
	ld	e, iyl
	ex	de, hl
	call	__lshru
	push	bc
	pop	hl
	ld	bc, 0
	ld	c, d
	ld	d, b
	ld	e, a
	ld	a, d
	call	__lcmpu
	jr	c, BB0_4
	push	bc
	pop	hl
BB0_4:
	ld	(ix - 3), hl
	ld	a, l
	dec	a
	ld	iy, (ix + 9)
	ld	c, a
	ld	d, (ix + 6)
BB0_5:
	or	a, a
	sbc	hl, hl
	ld	l, d
	ld	(ix - 9), hl
	ld	hl, (iy)
	ld	e, (iy + 3)
	dec	c
	ld	(ix - 6), hl
	call	__lcmpzero
	jr	nz, BB0_7
	lea	iy, iy + 4
	or	a, a
	ld	a, c
	jr	nz, BB0_5
BB0_7:
	ld	(ix - 10), e
	ld	de, (ix - 3)
	ld	hl, (ix - 9)
BB0_8:
	ld	iy, (ix + 18)
	ld	a, e
	cp	a, l
	jp	nc, BB0_15
	ld	(ix - 9), hl
	ld	c, 2
	push	de
	pop	hl
	call	__ishl
	push	de
	pop	bc
	push	hl
	pop	de
	ld	hl, (ix + 9)
	add	hl, de
	ld	de, (ix + 12)
	ld	a, e
	and	a, 31
	ld	iyl, a
	ld	e, (ix + 6)
	ld	a, e
	ld	(ix - 3), bc
	sub	a, c
	ld	c, a
	ld	a, iyl
	or	a, a
	jr	nz, BB0_13
	ld	c, e
	ex	de, hl
	ld	hl, (ix - 3)
	ld	a, l
	sub	a, c
	ld	l, a
	ld	bc, (ix + 18)
BB0_11:
	ld	a, l
	or	a, a
	jr	z, BB0_14
	push	de
	pop	iy
	ld	de, (iy)
	ld	(ix - 13), de
	ld	a, (iy + 3)
	lea	de, iy
	push	bc
	pop	iy
	ld	bc, (ix - 13)
	ld	(iy), bc
	ld	(iy + 3), a
	lea	bc, iy
	push	de
	pop	iy
	lea	iy, iy + 4
	lea	de, iy
	push	bc
	pop	iy
	lea	iy, iy + 4
	lea	bc, iy
	inc	l
	jr	BB0_11
BB0_13:
	ld	de, (ix + 18)
	push	de
	push	iy
	push	hl
	push	bc
	call	_softfloat_shortShiftRightJamM
	pop	hl
	pop	hl
	pop	hl
	pop	hl
	ld	hl, (ix - 3)
	ld	a, l
	or	a, a
	ld	hl, (ix - 6)
	jr	z, BB0_16
BB0_14:
	ld	hl, (ix - 9)
	ld	de, (ix - 3)
	or	a, a
	sbc	hl, de
	ld	c, 2
	call	__ishl
	push	hl
	pop	bc
	ld	iy, (ix + 18)
	add	iy, bc
BB0_15:
	ld	c, 2
	ex	de, hl
	call	__ishl
	push	hl
	or	a, a
	sbc	hl, hl
	push	hl
	push	iy
	call	_memset
	pop	hl
	pop	hl
	pop	hl
	ld	hl, (ix - 6)
BB0_16:
	ld	e, (ix - 10)
	call	__lcmpzero
	jr	z, BB0_18
	ld	bc, 1
	ld	hl, (ix + 18)
	push	hl
	pop	iy
	ld	hl, (iy)
	lea	iy, iy + 3
	ld	e, (iy)
	xor	a, a
	call	__lor
	ld	iy, (ix + 18)
	ld	(iy), hl
	ld	iy, (ix + 18)
	ld	(iy + 3), e
BB0_18:
	ld	sp, ix
	pop	ix
	ret
	section	.text,"ax",@progbits

	ident	"clang version 15.0.7 (https://github.com/CE-Programming/llvm-project 31b787336940cbc96d054033c3d843f344507042)"
	extern	_memset
	extern	__lor
	extern	__Unwind_SjLj_Register
	extern	__frameset
	extern	_softfloat_shortShiftRightJamM
	extern	__Unwind_SjLj_Unregister
	extern	__lcmpzero
	extern	__lcmpu
	extern	__lshru
	extern	__ishl
