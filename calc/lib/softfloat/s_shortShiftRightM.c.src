	section	.text,"ax",@progbits
	assume	adl = 1
	section	.text,"ax",@progbits
	public	_softfloat_shortShiftRightM
_softfloat_shortShiftRightM:
	ld	hl, -19
	call	__frameset
	ld	iy, (ix + 9)
	ld	e, (ix + 12)
	ld	a, e
	neg
	ld	bc, 0
	ld	c, (ix + 6)
	dec	bc
	ld	hl, (iy)
	ld	(ix - 9), hl
	ld	d, (iy + 3)
	and	a, 31
	ld	(ix - 19), a
	lea	hl, iy + 4
	ld	(ix - 6), hl
	ld	(ix - 18), bc
	ld	(ix - 3), bc
	ld	hl, (ix + 15)
	ld	(ix - 15), hl
BB0_1:
	ld	bc, (ix - 9)
	ld	a, d
	ld	l, e
	call	__lshru
	ld	(ix - 12), bc
	ld	d, a
	ld	hl, (ix - 3)
	add	hl, bc
	or	a, a
	sbc	hl, bc
	jr	z, BB0_3
	ld	iy, (ix - 6)
	ld	bc, (iy)
	ld	a, (iy + 3)
	ld	(ix - 9), bc
	ld	iyl, a
	ld	l, (ix - 19)
	call	__lshl
	push	bc
	pop	hl
	ld	e, a
	ld	bc, (ix - 12)
	ld	a, d
	ld	d, iyl
	call	__lor
	ld	iy, (ix - 15)
	ld	(iy), hl
	ld	(iy + 3), e
	lea	hl, iy
	ld	e, (ix + 12)
	ld	iy, (ix - 6)
	lea	iy, iy + 4
	ld	(ix - 6), iy
	push	hl
	pop	iy
	lea	iy, iy + 4
	ld	(ix - 15), iy
	ld	hl, (ix - 3)
	dec	hl
	ld	(ix - 3), hl
	jr	BB0_1
BB0_3:
	ld	c, 2
	ld	hl, (ix - 18)
	call	__ishl
	push	hl
	pop	bc
	ld	iy, (ix + 15)
	add	iy, bc
	ld	hl, (ix - 12)
	ld	(iy), hl
	ld	(iy + 3), d
	ld	sp, ix
	pop	ix
	ret
	section	.text,"ax",@progbits

	ident	"clang version 15.0.7 (https://github.com/CE-Programming/llvm-project 31b787336940cbc96d054033c3d843f344507042)"
	extern	__Unwind_SjLj_Register
	extern	__lor
	extern	__Unwind_SjLj_Unregister
	extern	__lshl
	extern	__frameset
	extern	__lshru
	extern	__ishl
