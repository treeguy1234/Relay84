	section	.text,"ax",@progbits
	assume	adl = 1
	section	.text,"ax",@progbits
	public	_softfloat_normRoundPackToF64
_softfloat_normRoundPackToF64:
	ld	hl, -18
	call	__frameset
	ld	iy, (ix + 9)
	ld	hl, (ix + 12)
	ld	de, (ix + 15)
	ld	bc, (ix + 18)
	call	__llctlz
	ld	e, a
	ld	c, e
	dec	c
	ld	a, c
	rlc	a
	sbc	hl, hl
	ld	(ix - 3), hl
	ld	a, c
	rlc	a
	sbc	a, a
	ld	b, a
	ex	de, hl
	ld	e, iyl
	ld	d, iyh
	ex	de, hl
	or	a, a
	sbc.sis	hl, bc
	ex	de, hl
	ld	iyl, e
	ld	iyh, d
	ex	de, hl
	ld	a, e
	cp	a, 11
	jr	c, BB0_4
	ld.sis	de, 2045
	ex	de, hl
	ld	e, iyl
	ld	d, iyh
	ex	de, hl
	or	a, a
	sbc.sis	hl, de
	jr	nc, BB0_4
	ld	a, (ix + 6)
	bit	0, a
	bit	0, a
	ld.sis	hl, 0
	bit	0, a
	jp	nz, BB0_5
	ld	e, l
	ld	d, h
	jp	BB0_6
BB0_4:
	ld	a, c
	rlc	a
	sbc	hl, hl
	push	hl
	pop	de
	ld	e, c
	push	de
	pop	hl
	ld	(ix - 3), iy
	push	de
	pop	iy
	add	iy, iy
	sbc	hl, hl
	push	hl
	pop	bc
	or	a, a
	sbc	hl, hl
	push	hl
	push	hl
	dec	hl
	push	hl
	ex	de, hl
	push	bc
	pop	de
	call	__lland
	pop	de
	pop	de
	pop	de
	push	hl
	ld	hl, (ix + 12)
	ld	de, (ix + 15)
	ld	bc, (ix + 18)
	call	__llshl
	pop	iy
	push	bc
	push	de
	push	hl
	ld	hl, (ix - 3)
	push	hl
	ld	l, (ix + 6)
	push	hl
	call	_softfloat_roundPackToF64
	ld	iy, 15
	add	iy, sp
	ld	sp, iy
	jp	BB0_16
BB0_5:
	ld.sis	de, -32768
BB0_6:
	ld	(ix - 12), de
	ld	(ix - 6), hl
	ld	(ix - 9), bc
	ld	hl, (ix + 12)
	ld	de, (ix + 15)
	ld	bc, (ix + 18)
	call	__llcmpzero
	jr	z, BB0_8
	ld	a, 0
	jr	BB0_9
BB0_8:
	ld	a, 1
BB0_9:
	or	a, a
	sbc	hl, hl
	ex	de, hl
	ld	e, iyl
	ld	d, iyh
	ex	de, hl
	ld	de, 0
	ld.sis	bc, 0
	ld	iy, 52
	push	iy
	call	__llshl
	pop	iy
	bit	0, a
	ld	iy, 0
	jr	nz, BB0_11
	push	hl
	pop	iy
BB0_11:
	bit	0, a
	ld	hl, 0
	jr	nz, BB0_13
	ex	de, hl
BB0_13:
	ld	(ix - 18), hl
	ld	(ix - 15), iy
	ld	iy, -10
	ld	hl, (ix - 9)
	ld	de, (ix - 3)
	ld	e, l
	ex	de, hl
	bit	0, a
	jr	nz, BB0_15
	ld	e, c
	ld	d, b
	ld	(ix - 6), de
BB0_15:
	lea	de, iy
	add	hl, de
	push	hl
	ld	hl, (ix + 12)
	ld	de, (ix + 15)
	ld	bc, (ix + 18)
	call	__llshl
	pop	iy
	ld	iy, (ix - 12)
	push	iy
	ld	iy, 0
	push	iy
	push	iy
	call	__lladd
	pop	iy
	pop	iy
	pop	iy
	ld	iy, (ix - 6)
	push	iy
	ld	iy, (ix - 18)
	push	iy
	ld	iy, (ix - 15)
	push	iy
	call	__lladd
	pop	iy
	pop	iy
	pop	iy
BB0_16:
	ld	sp, ix
	pop	ix
	ret
	section	.text,"ax",@progbits

	ident	"clang version 15.0.7 (https://github.com/CE-Programming/llvm-project 31b787336940cbc96d054033c3d843f344507042)"
	extern	__lland
	extern	__Unwind_SjLj_Register
	extern	__Unwind_SjLj_Unregister
	extern	__frameset
	extern	__lladd
	extern	_softfloat_roundPackToF64
	extern	__llshl
	extern	__llctlz
	extern	__llcmpzero
