	section	.text,"ax",@progbits
	assume	adl = 1
	section	.text,"ax",@progbits
	public	_boot_vsnprintf
_boot_vsnprintf:
	call	__frameset0
	ld	de, (ix + 12)
	ld	bc, (ix + 15)
	ld	hl, -1835008
	push	bc
	push	de
	push	hl
	call	_boot_vsprintf
	ld	iy, (ix + 6)
	push	hl
	pop	de
	pop	hl
	pop	hl
	pop	hl
	lea	hl, iy
	add	hl, bc
	or	a, a
	sbc	hl, bc
	jr	z, BB0_6
	ld	bc, (ix + 9)
	push	bc
	pop	hl
	add	hl, bc
	or	a, a
	sbc	hl, bc
	jr	z, BB0_6
	push	de
	pop	hl
	or	a, a
	sbc	hl, bc
	jr	nc, BB0_4
	ld	bc, 1
	push	de
	pop	hl
	or	a, a
	sbc	hl, bc
	call	pe, __setflag
	jp	p, BB0_5
BB0_4:
	ld	(iy), 0
	jr	BB0_6
BB0_5:
	ld	hl, (ix + 15)
	push	hl
	ld	hl, (ix + 12)
	push	hl
	push	iy
	call	_boot_vsprintf
	push	hl
	pop	de
	pop	hl
	pop	hl
	pop	hl
BB0_6:
	ex	de, hl
	pop	ix
	ret
	section	.text,"ax",@progbits

	section	.text,"ax",@progbits
	public	_boot_snprintf
_boot_snprintf:
	ld	hl, -3
	call	__frameset
	ld	iy, (ix + 6)
	ld	de, (ix + 9)
	ld	bc, (ix + 12)
	lea	hl, ix + 15
	ld	(ix - 3), hl
	ld	hl, (ix - 3)
	push	hl
	push	bc
	push	de
	push	iy
	call	_boot_vsnprintf
	ld	sp, ix
	pop	ix
	ret
	section	.text,"ax",@progbits

	section	.text,"ax",@progbits
	public	_boot_vasprintf
_boot_vasprintf:
	call	__frameset0
	ld	hl, (ix + 6)
	ld	bc, (ix + 9)
	ld	de, (ix + 12)
	ld	iy, 0
	ld	(hl), iy
	push	de
	push	bc
	ld	hl, -1835008
	push	hl
	call	_boot_vsprintf
	push	hl
	pop	de
	pop	hl
	pop	hl
	pop	hl
	ld	bc, 0
	push	de
	pop	hl
	or	a, a
	sbc	hl, bc
	call	pe, __setflag
	jp	m, BB2_3
	inc	de
	push	de
	call	_malloc
	pop	de
	ld	iy, (ix + 6)
	ld	(iy), hl
	add	hl, bc
	or	a, a
	sbc	hl, bc
	ld	de, -1
	jr	z, BB2_3
	ld	de, (ix + 12)
	push	de
	ld	de, (ix + 9)
	push	de
	push	hl
	call	_boot_vsprintf
	push	hl
	pop	de
	pop	hl
	pop	hl
	pop	hl
BB2_3:
	ex	de, hl
	pop	ix
	ret
	section	.text,"ax",@progbits

	section	.text,"ax",@progbits
	public	_boot_asprintf
_boot_asprintf:
	ld	hl, -3
	call	__frameset
	ld	hl, (ix + 6)
	ld	de, (ix + 9)
	lea	bc, ix + 12
	ld	(ix - 3), bc
	ld	bc, (ix - 3)
	push	bc
	push	de
	push	hl
	call	_boot_vasprintf
	ld	sp, ix
	pop	ix
	ret
	section	.text,"ax",@progbits

	ident	"clang version 15.0.7 (https://github.com/CE-Programming/llvm-project 31b787336940cbc96d054033c3d843f344507042)"
	extern	__setflag
	extern	__Unwind_SjLj_Register
	extern	__Unwind_SjLj_Unregister
	extern	_boot_vsprintf
	extern	__frameset
	extern	_malloc
	extern	__frameset0
