	section	.text,"ax",@progbits
	assume	adl = 1
	section	.text,"ax",@progbits
	public	___hypot3f
___hypot3f:
	ld	hl, -21
	call	__frameset
	ld	hl, (ix + 6)
	ld	e, (ix + 9)
	push	de
	push	hl
	call	_fabsf
	ld	(ix - 12), hl
	ld	(ix - 15), e
	pop	hl
	pop	hl
	ld	l, (ix + 15)
	push	hl
	ld	hl, (ix + 12)
	push	hl
	call	_fabsf
	ld	(ix - 9), hl
	ld	(ix - 6), e
	pop	hl
	pop	hl
	ld	l, (ix + 21)
	push	hl
	ld	hl, (ix + 18)
	push	hl
	call	_fabsf
	push	hl
	pop	bc
	ld	iyl, e
	pop	hl
	pop	hl
	ld	hl, (ix - 9)
	ld	e, (ix - 6)
	ld	a, iyl
	call	__lcmpu
	ccf
	sbc	a, a
	inc	a
	bit	0, a
	ld	hl, (ix - 9)
	jr	nz, BB0_2
	push	bc
	pop	hl
BB0_2:
	bit	0, a
	ld	e, (ix - 6)
	jr	nz, BB0_4
	ld	e, iyl
BB0_4:
	bit	0, a
	jr	nz, BB0_6
	ld	bc, (ix - 9)
BB0_6:
	ld	(ix - 21), de
	ld	(ix - 18), hl
	bit	0, a
	jr	nz, BB0_8
	push	af
	ld	a, (ix - 6)
	ld	iyl, a
	pop	af
BB0_8:
	ld	hl, (ix - 12)
	ld	e, (ix - 15)
	ld	(ix - 6), iy
	ld	a, iyl
	call	__lcmpu
	ccf
	sbc	a, a
	inc	a
	bit	0, a
	jr	nz, BB0_10
	push	bc
	pop	hl
BB0_10:
	bit	0, a
	ld	iyl, e
	ld	(ix - 9), iy
	jp	nz, BB0_12
	ld	iy, (ix - 6)
	ld	(ix - 9), iy
BB0_12:
	bit	0, a
	jr	nz, BB0_14
	ld	bc, (ix - 12)
BB0_14:
	ld	(ix - 12), hl
	bit	0, a
	ld	hl, (ix - 6)
	jr	nz, BB0_16
	ld	l, e
BB0_16:
	ld	(ix - 6), hl
	push	hl
	push	bc
	ld	(ix - 15), bc
	call	__isfinitef
	pop	hl
	pop	hl
	bit	0, a
	jp	z, BB0_18
	pea	ix - 3
	ld	hl, (ix - 6)
	push	hl
	ld	hl, (ix - 15)
	push	hl
	call	_frexpf
	ld	(ix - 6), hl
	ld	(ix - 15), e
	pop	hl
	pop	hl
	pop	hl
	ld	hl, (ix - 3)
	call	__ineg
	push	hl
	ld	hl, (ix - 9)
	push	hl
	ld	hl, (ix - 12)
	push	hl
	call	_ldexpf
	ld	(ix - 9), hl
	ld	(ix - 12), e
	pop	hl
	pop	hl
	pop	hl
	ld	hl, (ix - 3)
	call	__ineg
	push	hl
	ld	hl, (ix - 21)
	push	hl
	ld	hl, (ix - 18)
	push	hl
	call	_ldexpf
	pop	bc
	pop	bc
	pop	bc
	push	hl
	pop	bc
	ld	a, e
	call	__fmul
	push	bc
	pop	iy
	ld	d, a
	ld	bc, (ix - 9)
	ld	a, (ix - 12)
	push	bc
	pop	hl
	ld	e, a
	call	__fmul
	lea	hl, iy
	ld	e, d
	call	__fadd
	push	bc
	pop	iy
	ld	d, a
	ld	bc, (ix - 6)
	ld	a, (ix - 15)
	push	bc
	pop	hl
	ld	e, a
	call	__fmul
	lea	hl, iy
	ld	e, d
	call	__fadd
	ld	l, a
	push	hl
	push	bc
	call	_sqrtf
	pop	bc
	pop	bc
	ld	bc, (ix - 3)
	push	bc
	push	de
	push	hl
	call	_ldexpf
	pop	bc
	pop	bc
	pop	bc
	jr	BB0_24
BB0_18:
	ld	hl, (ix - 9)
	push	hl
	ld	hl, (ix - 12)
	push	hl
	call	__isinff
	pop	hl
	pop	hl
	bit	0, a
	jr	z, BB0_20
	ld	hl, -8388608
	ld	e, 127
	jr	BB0_24
BB0_20:
	ld	hl, (ix - 21)
	push	hl
	ld	hl, (ix - 18)
	push	hl
	call	__isinff
	pop	hl
	pop	hl
	bit	0, a
	ld	hl, -8388608
	jr	nz, BB0_22
	ld	hl, (ix - 15)
BB0_22:
	bit	0, a
	ld	bc, (ix - 6)
	ld	e, 127
	jr	nz, BB0_24
	ld	e, c
BB0_24:
	ld	sp, ix
	pop	ix
	ret
	section	.text,"ax",@progbits

	section	.text,"ax",@progbits
	public	___hypot3
	label	___hypot3 at ___hypot3f

	ident	"clang version 15.0.7 (https://github.com/CE-Programming/llvm-project 31b787336940cbc96d054033c3d843f344507042)"
	extern	_fabsf
	extern	_ldexpf
	extern	__Unwind_SjLj_Register
	extern	__frameset
	extern	__Unwind_SjLj_Unregister
	extern	__fadd
	extern	__ineg
	extern	_frexpf
	extern	_sqrtf
	extern	__isinff
	extern	__lcmpu
	extern	__isfinitef
	extern	__fmul
